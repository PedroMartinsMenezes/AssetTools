namespace AssetTool
{
    [JsonAsset("AnimNextWorldSubsystem")]
    public class UAnimNextWorldSubsystem : UWorldSubsystem
    {
        public override ITransferable Move(Transfer transfer)
        {
            return base.Move(transfer);
        }
    }
}