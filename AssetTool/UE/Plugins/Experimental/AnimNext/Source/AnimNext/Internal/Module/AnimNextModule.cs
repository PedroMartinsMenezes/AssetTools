namespace AssetTool
{
    [JsonAsset("AnimNextModule")]
    public class UAnimNextModule : UAnimNextDataInterface
    {
        public override ITransferable Move(Transfer transfer)
        {
            return base.Move(transfer);
        }
    }
}